1. 회귀방식
mse(mean squared error, 평균 제곱 오차

머신러닝 뿐만 아니라 영상처리 영역에서도 자주 사용되는 추측값에 대한 정확성 측정 방법. 
손실함수는 정답에 대한 오류를 숫자로 나타내는 것으로
오답에 가까울수록 큰 값이 나온다. 반대로 정답에 가까울수록 작은 값이 나온다.
mse는 회귀 지표

2. 분류방식

결괏값에 대한 분류가 정해져 있어야 함
y값에 대한 분류의 범위를 정해 놓아야 함

ex) y는 홀수 아니면 짝수

ex) 강아지 고양이 분류 모델을 만든다
강아지 = 0, 고양이 = 1

ex) 사람을 (생물학적) 성별로 분류
남자 = 1, 여자 = 2

즉, y와 ^y, 이 둘은 모두 내가 정해놓은 분류에 들어가야 함

ex)
꽃 분류방식
이파리에 따라 세 종류로 나눈다
손 글씨
손으로 쓴 글씨 0~9까지 10가지 종류로 분류

실제값과 예측값이 정해놓은 범위에 들어가야 함


loss = mse


회귀 = 선형
수치상으로 결론이 나옴

분류
동전 앞면 뒷면 0 아니면 1
0.5는 나올 수 없다
y값이 고정되어 있음 0 아니면 1
^y도 0 아니면 1

값을 고정하는 건 우리가 하는 일
남자 여자 외계인 이렇게 설정 가능
y값 0, 1, 2
모델 y값은 0,1,2 중 하나가 나옴

keras03파일의 문제점

11과 11.0001
회귀모델에선 o
분류모델에선 x

mse : 회귀모델
acc : 분류모델

loss : mse
metrics : acc

keras03은 회귀모델인데
metrics에 분류모델의 지표인 acc를 넣으니 문제가 있는 것

성능평가방식 






